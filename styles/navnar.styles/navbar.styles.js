import styled from 'styled-components';

export const StyledNav = styled.nav`
  .section-center {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 2rem 0;
    div {
      h2 {
        cursor: pointer;
      }
      /* button {
        font-size: 1.5rem;

        span {
          position: relative;
          top: -13px;
          left: 10px;
          min-height: 18px;
          min-width: 18px;
          padding: 3px;
          border-radius: 50%;
          background: red;
          font-size: 0.8rem;
          font-weight: 600;
          color: white;
        }
      } */
    }
  }
`;
// cart - btn;
